# Python
.venv/
__pycache__/
*.pyc

# data — raw pulls are gitignored, snapshots are committed deliberately
data/raw/
# per-URL LLM response cache: ephemeral, regenerable, not demo-critical
data/snapshots/events/cache/

# secrets
.env
.env.*
!.env.example

# Claude Code harness state
.claude/

# OS
.DS_Store
Thumbs.db
